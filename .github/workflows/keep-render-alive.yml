name: Keep Systems Awake

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -sSf https://exam-system-2k9t.onrender.com/ping > /dev/null || echo "Ping failed"
